# TCS UI 修正 TODO List

| 項目 | 日期 | 狀態 |
|---|---|---|
| 建立日 | 2026-05-07 | — |
| 完成日 | 2026-05-08 | ✅ 全項目完成 |
| 依據 | `docs/superpowers/specs/2026-05-06-tcs-design.md` §7-2、§7-3、§6-3、§11 |

---

## 規格釐清紀錄（2026-05-08 與使用者確認）

| # | 議題 | 結論 | 影響 |
|---|---|---|---|
| 1 | spec §5 vs 實作 API 命名（單數+`requirements` vs 複數+`plants`） | **沿用現行實作**：`/api/licenses`、`/api/licenses/{lt}/plants`、`/api/training-headers`、`/api/training-details/{e}/{l}/{date}`、`/api/employees`、`/api/plants`、`/api/export/training-headers` | 本 TODO 內 `GET /api/licenses/{licenseType}/requirements` 之描述以實作為準改為 `/plants`；spec 文件待後續同步 |
| 2 | spec §7-3 頁面路徑 `/Training` vs 實作 `/TrainingHeader` | **改為 `/Training`（依 spec）** | `TrainingHeaderController` (MVC view) → `TrainingController`；`Views/TrainingHeader/` → `Views/Training/`；`_Layout` 導覽列同步更新 |
| 3 | LicenseType 大類/小類自動判斷邏輯位置 | **純前端 regex 判斷**（`^\d+$` = 大類；`^\d+(\.\d+)+$` = 小類） | 不新增後端端點；Modal 開啟時即時切換 Category 顯示/必填星號 |
| 4 | spec §7-3 「EmployeeId 自動補完」實作方式 | **HTML5 `<datalist>`**（lazy-load 一次撈全部員工約 2000 筆，使用者已確認效能可接受） | 不引入第三方 typeahead 套件；不實作 spec §5-5 `?keyword=` 後端端點 |
| 5 | spec §7-3 「LicenseType 下拉，僅小類」資料來源 | **前端過濾**（`/api/licenses?pageSize=9999` 後 `filter(x => !x.IsCategory)`） | 不擴充後端 service 簽章；與大類/小類前端判斷邏輯一致 |

---

## Code Review 發現的 Bug

| 嚴重度 | 位置 | 問題 | 狀態 |
|---|---|---|---|
| Critical | `Create.cshtml:31`、`Edit.cshtml:31` | `<a href="/License">` 未加 PathBase，透過 Gateway 會 404 | ✅ 已修（後 Create.cshtml/Edit.cshtml 因 P1-1 移除 Modal 已整批刪除） |
| Critical | `Edit.cshtml:39` | `@Html.Raw(Html.Encode(Model))` — Spec §11 明確禁用 `@Html.Raw()`；`Html.Encode` 是 HTML 編碼而非 JS 字串編碼，應改用 `data-*` attribute | ✅ 已修為 `data-license-type` + `$('#edit-form').data('licenseType')`；後因 P1-1 整批刪除 |
| Critical | `license.js` / `training.js` renderTable | template literal 直接拼 HTML → XSS；Spec §11：「jQuery 一律用 `.text()` 顯示資料」 | ✅ 已重寫，全部改用 `.text()` / `.val()` 設值 |
| Minor | `_Layout.cshtml:11` | Navbar brand `href="/"` 未加 PathBase | ✅ 已修為 `href="@(Context.Request.PathBase)/"` |
| Minor | `license.js` showError | `console.error(msg)` 使用者看不到錯誤 | ✅ 已改為 Toast.error（P3-2）+ `#error-msg` alert div 雙保險 |
| Minor | `Create.cshtml` Hours/Years | `min="0"` 應為 `min="1"`（Spec §9：小類列 Hours > 0） | ✅ 已修；後因 P1-1 改為 Modal，Modal 內 input 同樣 `min="1"` |
| Minor | `training.js` viewDetails | 導向 `/TrainingHeader/Details/...`，路由不存在 → 404 | ✅ 已移除「詳細」按鈕；P1-2 改為選列後載入單身子表 |

---

## P1 — 頁面骨架

### [P1-1] `/License` 改為單頭 + 單身雙面板（Spec §7-2）✅ 完成

- [x] 移除 `Create`/`Edit` 分頁，改為 Bootstrap Modal（新增、修改共用同一個 Modal）
- [x] Modal 欄位：
  - [x] `LicenseType`：新增可填、修改 readonly；驗證格式為純整數（大類）或含小數點（小類）
  - [x] `Description`：必填、≤ 70
  - [x] `Category`：下拉，僅列大類（`LicenseType` 為純整數的列）；LicenseType 為純整數時自動隱藏並設 NULL
  - [x] `Hours`：小類必填 `> 0`；大類可選
  - [x] `Years`：小類必填 `≥ 1`；大類可選
- [x] 表格列點擊 → 整列高亮（藍底 + 左豎線）；切換列自動清除前一列
- [x] 按鈕列改為 `[新增] [修改] [刪除]`，修改/刪除預設 disabled，選列後 enabled
- [x] 表格下方加入**廠別需求子表**（LicensePlantRequirement）：
  - [x] 選列後 AJAX 載入（`GET /api/licenses/{licenseType}/plants` — 路徑沿用實作，見釐清 #1）
  - [x] 子表欄位：廠別代碼、廠別名稱、需求數
  - [x] 子表操作按鈕：`[+] [✎] [✗]`，各自 Modal（廠別下拉源自 `/api/plants`）
- [x] 刪除已不需要的 `LicenseViewController.Create()` / `Edit()` action
- [x] 刪除 `Views/License/Create.cshtml`、`Views/License/Edit.cshtml`（git rm）

### [P1-2] `/Training` 改為單頭 + 單身雙面板（Spec §7-3）✅ 完成

> 頁面路徑由 `/TrainingHeader` 改為 `/Training`（見釐清 #2）

- [x] 單頭表格補欄：
  - [x] 員編、姓名、部門、到職日（來自 Employee：MV001 / MV002 / MV004 / MV021）
  - [x] 證照、描述、應訓時數
  - [x] 最新回訓日（`LatestRetrainDate`）
  - [x] 未達時數（`RemainingHours`）
  - [x] 下次回訓（`NextReviewDate`）
  - [x] 備註
- [x] 移除「詳細」按鈕導頁，改為選列後 AJAX 載入**受訓單身子表**（TrainingDetail）
  - [x] 子表欄位：日期、類型（取得證照 / 回訓）、時數、是否過期
  - [x] 子表操作按鈕：`[+] [✎] [✗]`，各自 Modal
- [x] 單頭 Modal：
  - [x] 新增：`EmployeeId`（HTML5 datalist 自動補完，見釐清 #4）、`LicenseType`（下拉，僅小類；前端過濾，見釐清 #5）、`Remark`；`RequiredHours` 系統自動帶入 readonly
  - [x] 修改：僅可改 `Remark`，其餘 readonly
- [x] 單身 Modal：`TrainingDate` / `TrainingType`（單選：取得證照 / 回訓） / `Hours`
- [x] 按鈕列：`[新增] [修改] [刪除] [匯出 Excel]`；修改/刪除預設 disabled，選列後 enabled
- [x] 單身異動後自動重撈單頭頁，使衍生欄位（IsExpired / RemainingHours）即時更新

---

## P2 — P1 完成後接續

### [P2-1] 修正 Code Review Bug ✅ 完成（先於 P1 完成，作為穩定底）

- [x] `_Layout.cshtml` brand href 加 PathBase
- [x] `renderTable`（license.js / training.js）所有欄位改用 jQuery `.text()` / `.val()` 設值，不用 template literal 拼 HTML
- [x] `showError` / API 錯誤回應改為頁面可見訊息（P3-2 Toast 完成後改走 Toast.error）

### [P2-2] 按鈕依 JWT `action` 欄位控制 disabled（Spec §6-3）✅ 完成

- [x] 建立 `wwwroot/js/tcs-auth.js` 共用 helper：`getActions()` / `hasAction()` / `applyButtonGuards()` / `enableIfAllowed()`
- [x] 處理 base64url 解碼 + UTF-8（`action` 為中文）
- [x] 頁面載入後解析 `sessionStorage.erp_auth_token` 的 payload
- [x] 依 `action` 欄位 disable 對應按鈕：

  | 按鈕 | disabled 條件 |
  |---|---|
  | 新增 / 子表 ＋ | 不含 `新增` |
  | 修改 / 子表 ✎ | 不含 `修改` |
  | 刪除 / 子表 ✗ | 不含 `刪除` |
  | 匯出 Excel | 不含 `儲存` |

- [x] 無權限按鈕標 `data-action-denied="true"` + hover title「無「X」權限」
- [x] 選列後 enable 邏輯改用 `TcsAuth.enableIfAllowed`，避免覆蓋權限守門

### [P2-3] Modal 的 Category 欄位改為下拉 ✅ 完成（併入 P1-1）

- [x] Modal 開啟時從已載入資料過濾 `LicenseType` 為純整數的列作為下拉選項（含 `IsCategory` flag 與前端 regex 雙保險）
- [x] LicenseType 欄位輸入純整數時，Category 欄位自動隱藏並清空，送出時 Category=null

---

## P3 — 次要 ✅ 完成

### [P3-1] 兩段式搜尋（Spec §7-1）✅ 完成

**後端：**
- [x] 新增 `LicenseSearchQuery` / `TrainingSearchQuery` DTO（`TCS.Core/DTOs/Requests/`）
- [x] `LicenseService.GetAllAsync` / `TrainingService.GetHeadersAsync` 加 `query` 參數，套兩段式邏輯（進階生效時忽略 quick search）
- [x] `LicenseController` / `TrainingHeaderController` / `ExportController` 加 `[FromQuery] *SearchQuery? query`
- [x] 衍生欄位（`OverallStatus.已過期`、`RemainingHours > 0`、`NextReviewDate` 區間）在 DTO 投影後 in-memory 過濾

**前端：**
- [x] `/License` 加入「進階搜尋 ▾」折疊面板
  - [x] 欄位：證照類別（前綴 LIKE）、描述（LIKE）、大類（下拉）、時數區間、年數區間
- [x] `/Training` 加入「進階搜尋 ▾」折疊面板
  - [x] 欄位：員工編號、姓名（LIKE）、部門（下拉，自員工資料去重）、證照類別（下拉，僅小類）、僅顯示已過期、未達時數 > 0、下次回訓區間
- [x] 兩段式互斥：點任一邊會清空另一邊（spec §7-1）
- [x] 匯出 Excel 沿用當前進階/快速搜尋條件
- [x] License 大類下拉用獨立 `cachedAllCategories` 一次性載入，不被當前頁篩選結果干擾

### [P3-2] Toast 通知（Spec §7-1）✅ 完成

- [x] 建立 `wwwroot/js/toast.js` 共用 helper：`Toast.success` / `Toast.error` / `Toast.info`
- [x] 動態插入 `#toast-container`（fixed bottom-end），無需在 layout 預埋
- [x] 成功：綠底 Toast，3 秒自動消失
- [x] 失敗：紅底 Toast，手動關閉
- [x] 替換現行所有 `alert()` 與 `console.error()`（Modal 內 client-side validation 仍走 modal alert div，UX 較佳）

---

## 完成摘要

**新增檔案：**
- `src/TCS.Web/wwwroot/js/tcs-auth.js`
- `src/TCS.Web/wwwroot/js/toast.js`
- `src/TCS.Core/DTOs/Requests/LicenseSearchQuery.cs`
- `src/TCS.Core/DTOs/Requests/TrainingSearchQuery.cs`

**改名：**
- `src/TCS.Web/Controllers/TrainingHeaderViewController.cs` → `TrainingViewController.cs`（class `TrainingHeaderController` → `TrainingController`）
- `src/TCS.Web/Views/TrainingHeader/` → `src/TCS.Web/Views/Training/`

**刪除：**
- `src/TCS.Web/Views/License/Create.cshtml`
- `src/TCS.Web/Views/License/Edit.cshtml`

**修改：**
- `src/TCS.Web/Views/Shared/_Layout.cshtml`（PathBase、加載 tcs-auth.js / toast.js、nav 改 /Training）
- `src/TCS.Web/Views/License/Index.cshtml`（雙面板 + 進階搜尋 + Modal）
- `src/TCS.Web/Views/Training/Index.cshtml`（雙面板 + 進階搜尋 + Modal）
- `src/TCS.Web/wwwroot/js/license.js`（重寫）
- `src/TCS.Web/wwwroot/js/training.js`（重寫）
- `src/TCS.Web/Controllers/LicenseViewController.cs`（移除 Create/Edit action）
- `src/TCS.Web/Controllers/LicenseController.cs`（API 加 `[FromQuery] LicenseSearchQuery`）
- `src/TCS.Web/Controllers/TrainingHeaderController.cs`（API 加 `[FromQuery] TrainingSearchQuery`）
- `src/TCS.Web/Controllers/ExportController.cs`（API 加 `[FromQuery] TrainingSearchQuery`）
- `src/TCS.Core/Interfaces/ILicenseService.cs`、`ITrainingService.cs`（簽章擴充）
- `src/TCS.Core/Services/LicenseService.cs`、`TrainingService.cs`（in-memory 過濾邏輯）

**驗證：**
- `dotnet build` 0 errors / 2 warnings（NU1902 OpenTelemetry 與本變更無關）
- `dotnet test` 102 / 102 passed

