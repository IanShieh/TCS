# TCS 廠別需求總覽頁 TODO List

| 項目 | 日期 | 狀態 |
|---|---|---|
| 建立日 | 2026-07-09 | — |
| 完成日 | — | 進行中 |
| 依據 | 承接 todo-2026-07-08「T3 廠別需求總覽頁（方案 C）」，該案 T1/T2/T4 已完成並推送（135 tests） |

---

## T3 — 廠別需求總覽頁（採方案 C）

> 單身 `LicensePlantRequirement` 記錄廠別需求，但目前必須逐筆點選單頭才能在子表看到。
> 使用者需求是以**廠別**為主詞查詢（「A1 廠需要哪些證照、各要幾人」），
> 證照主軸的增強（原方案 A 列表摘要欄、方案 B 進階搜尋條件）無法直接回答，均不採用。
> **決策（2026-07-08）：採方案 C** — 新增「廠別需求總覽」頁，資料反向轉軸，零 schema 變更。

### 設計考量：CRUD 維護入口放在本頁（2026-07-08 討論定案）

**議題**：總覽頁做成唯讀，使用者會不會預期「在這裡直接維護」？

**結論：第一階段就內建 CRUD，本頁成為廠別需求的唯一維護入口**，理由：

1. **使用者心理預期** — 使用者預期在「看得到資料的地方」直接操作；廠別視角頁面唯讀會產生
   「那我要去哪改？」的困惑（此為本議題的起因）
2. **維護動線跟著思考單位走** — 實務情境是「A1 廠盤點：堆高機 3 人、天車 2 人」，
   在本頁「選廠 → 逐筆加證照」一氣呵成；在證照管理做同一件事要逐張證照跳頁操作
3. **避免尷尬中間態** — 先上唯讀版，等於：使用者困惑一輪 → 改版加 CRUD → 使用者重新學一次；
   不如第一版直接做進去
4. **成本不高** — 後端 CRUD API 已存在（`api/licenses/{licenseType}/plants`，
   `LicensePlantRequirementController`），本頁直接呼叫即可，後端零改動；
   前端照 `license.js` 廠別需求子表模式做 modal，權限沿用 `TcsAuth.applyButtonGuards` + 既有 `[RequireAction]`

**配套原則：維護入口只能有一個** — 同一份資料兩套編輯 UI 是最差方案
（使用者不確定該去哪改、表單邏輯與測試要維護兩份）。
故證照管理單身**降級為唯讀檢視**（檢視價值仍在：選證照順便看哪些廠需要），並加提示導向本頁維護。

**待與使用者確認**：是否存在「證照驅動」的維護情境（如新開證照時順手設定適用廠別）？
若有，改在本頁反向操作（每廠各加一筆）可完成、僅多幾個點擊，需確認使用者接受此 trade-off。
> **已確認（2026-07-09）：可捨，照計畫降級**——證照驅動情境改在總覽頁反向操作。

### 範圍

- **第一階段（本次）**：廠別需求列表 + CRUD 維護 — 選廠別 → 檢視/新增/修改/刪除該廠的證照需求；
  證照管理單身同步降級唯讀
- **第二階段（另立需求）**：需求 vs 實際持照人數缺口分析 — 需先與使用者確認「實際持照」計算口徑
  （依 `TrainingHeader.Plant`？已過期算不算？）；證照 × 廠別矩陣、Excel 匯出亦後放

### [T3-1] 後端 — DTO

- [x] `src/TCS.Core/DTOs/` 新增 `PlantRequirementOverviewDto`（`LicenseType`、`Description`、`RequiredCount`）
      （`Description` 為 nullable，防禦主檔缺漏；映射走 `MappingExtensions.ToOverviewDto`）

### [T3-2] 後端 — Service / Repository（依 T3-0 結論：走標準流程）

- [x] `ILicenseRepository` 加 `GetPlantRequirementsByPlantAsync(string plant)`：依廠別撈 `LicensePlantRequirement`
      （`Include(LicenseMasterNav)` 帶證照主檔；`Plant`(MB002) 為 `char(10)`，SQL Server 等值比較忽略尾端空白，參數毋須補齊）
- [x] `ILicenseService` 加 `GetRequirementsByPlantAsync(string plant)`：映射成 `PlantRequirementOverviewDto`，
      依 `LicenseTypeSort.NaturalKey` 自然排序（與證照頁一致）— 有測試防護（2 條：映射+排序、空清單）

### [T3-3] 後端 — API Controller（依 T3-0 結論：擴充 `PlantController`）

- [x] `PlantController` 多注入 `ILicenseService`，新增 `GET /api/plants/{plant}/requirements`
- [x] 沿用類別上的 `[Authorize]`；唯讀查詢不加 `[RequireAction]`

### [T3-4] 前端 — 頁面（檢視 + CRUD）

- [x] 新增 View Controller（`PlantRequirementViewController.cs`，class `PlantRequirementController`）+ `Views/PlantRequirement/Index.cshtml`，路由 `/PlantRequirement`
- [x] `_Layout.cshtml` 導覽列加「廠別需求總覽」連結
- [x] 新增 `wwwroot/js/plantRequirement.js`：廠別下拉（`GET /api/plants`）→ 載入需求表格
- [x] CRUD modal（照 `license.js` 廠別需求子表模式）：新增時挑證照 + 填需求人數；修改僅人數；刪除加確認
      （證照下拉沿用現行語意 = 證照主檔全清單，大類/小類皆可掛需求；重複由後端 409 攔截）
- [x] 寫入走既有 API `api/licenses/{licenseType}/plants`（`LicensePlantRequirementController`），後端零改動
- [x] 按鈕權限：`TcsAuth.applyButtonGuards`（新增/修改/刪除）；新增另需先選廠別、修改/刪除需先選列

### [T3-6] 前端 — 證照管理單身降級唯讀

- [x] `src/TCS.Web/Views/License/Index.cshtml` — 移除廠別需求子表的新增/修改/刪除按鈕與 modal
- [x] `src/TCS.Web/wwwroot/js/license.js` — 移除子表 CRUD 相關程式（modal/送出/刪除/選列），保留 `loadRequirements` 檢視
- [x] 子表區塊加提示文字/連結：「唯讀檢視——維護請至『廠別需求總覽』」
- [x] **執行前先完成上方「待與使用者確認」事項**（2026-07-09 已確認可捨）

### [T3-7] 驗收調整（2026-07-09 使用者回饋）

- [x] 需求量下限 0 → 1：`CreateLicensePlantRequirementValidator` / `UpdateLicensePlantRequirementValidator`
      改 `GreaterThanOrEqualTo(1)`，`plantRequirement.js` 前端檢查與 input `min` 同步；驗證測試改為 0 invalid / 1 valid
- [x] 頁面名稱「廠別需求總覽」→「廠別需求」（`Index.cshtml` 標題、`_Layout` 導覽列、證照管理子表導向連結文字）

### [T3-5] 驗證

- [x] `dotnet build` 0 errors、`dotnet test` 全綠（135 既有 + 2 新 = 137/137；驗收調整後 139/139）
- [ ] 選廠別 → 正確列出該廠需求證照與人數，與證照管理子表資料一致（待頁面實測）
- [ ] 本頁新增/修改/刪除需求 → 證照管理子表同步反映（同一份資料）（待頁面實測）
- [ ] 新增重複（同證照+同廠）→ 後端 409 正常攔截並顯示訊息（待頁面實測）
- [ ] 無需求的廠別顯示空狀態（待頁面實測）
- [ ] 未登入 → API 回 401；無新增/修改/刪除 action 的帳號 → 對應按鈕停用（待頁面實測）
- [ ] 證照管理單身僅剩檢視，提示連結可導向總覽頁（待頁面實測）

### [T3-0] 架構定義討論（已定案 2026-07-08）

- [x] **反向查詢歸屬**：掛在 `ILicenseService.GetRequirementsByPlantAsync(plant)`（廠別需求屬 License 域；
      第二階段缺口分析需跨 `ILicenseRepository` + `ITrainingRepository` 協調，Service 層是必然落點）
- [x] **讀取分層規則（全案適用，正式定義）**：
      純搬資料給人看、零判斷邏輯的唯讀查詢 → 允許 Controller 直接打 Repository（追認 `PlantController` 現況合法）；
      涉及跨表組裝、或可預期會長出業務規則者 → 一律走 Controller → Service → Repository 標準流程。
      **本功能屬後者**（join `LicenseMaster` + 第二階段缺口分析），走標準流程
- [x] **權限**：唯讀查詢僅 `[Authorize]`，不需新 JWT action；路由掛 `GET /api/plants/{plant}/requirements`
      （與 `api/licenses/{licenseType}/plants` 形成雙軸對稱），實作擴充 `PlantController` 多注入 `ILicenseService`

---

## 驗證清單

- [x] `dotnet build` 0 errors
- [x] `dotnet test` 全綠（139/139）
- [ ] T3：廠別需求總覽頁完成（第一階段列表 + CRUD）（程式完成；瀏覽器待實測）
- [ ] 證照管理單身降級唯讀，提示連結導向總覽頁（程式完成；瀏覽器待實測）
