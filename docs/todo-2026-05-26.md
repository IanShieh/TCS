# TCS 修正 TODO List

| 項目 | 日期 | 狀態 |
|---|---|---|
| 建立日 | 2026-05-26 | — |
| 完成日 | 2026-05-26 | 已完成 |
| 依據 | 使用者回報（2026-05-26） |

---

## 規格釐清紀錄

| # | 議題 | 結論 | 影響 |
|---|---|---|---|
| 1 | `TrainingDetail.Hours` nullable 是否正確？ | **決定改為 nullable = true**（路線 B）；允許先建紀錄、事後補時數；DB 欄位由使用者自行修改 | 需修改 entity、DTO、Request、Validator（見 T2） |

---

## T1 — 修正員工搜尋 404 (`/api/employees/search`)

> `training.js` 中有兩處 `fetch` 呼叫未使用 `BASE` 前綴，直接寫死路徑。
> `Program.cs` 設定 `UsePathBase("/tcs")`，Razor Layout 對應設定 `window.ERP_BASE = '/tcs'`，
> 導致這兩處呼叫不帶 `/tcs` 前綴，命中不存在的路由而回傳 404。

### 根本原因

`src/TCS.Web/wwwroot/js/training.js`：

| 行數 | 現狀（錯誤） | 應改為 |
|---|---|---|
| 169 | `` fetch(`/api/employees/search?q=${...}`) `` | `` fetch(`${EMPLOYEE_API}/search?q=${...}`) `` |
| 258 | `` fetch(`/api/employees/${encodeURIComponent(item.EmployeeId)}`) `` | `` fetch(`${EMPLOYEE_API}/${encodeURIComponent(item.EmployeeId)}`) `` |

`EMPLOYEE_API` 已於第 4 行定義為 `BASE + '/api/employees'`，統一使用即可。

### [T1-1] 修正 `training.js` 兩處硬編碼路徑

- [x] 將 `training.js:169` 改為 `` `${EMPLOYEE_API}/search?q=${encodeURIComponent(q)}` ``
- [x] 將 `training.js:258` 改為 `` `${EMPLOYEE_API}/${encodeURIComponent(item.EmployeeId)}` ``

### [T1-2] 驗證

- [x] 開啟 Training 頁面，員工 Picker 輸入 `2213`，Network tab 確認請求路徑含 `/tcs` 前綴
- [x] 確認回傳員工清單且 picker 正常顯示
- [x] 開啟既有 Training Header 進入 Edit 模式，確認 `training.js:258` 的 GET 也正常取得員工資料

---

## T2 — `TrainingDetail.Hours` 改為 nullable (`decimal?`)

> schema.json 由程式碼產生，`TB005 nullable: false` 反映 C# `decimal Hours`（值型別）。
> 決定改為 `decimal?`，允許訓練紀錄建立時不填時數、事後補入。
> DB 欄位由使用者自行 ALTER TABLE，不跑 EF Migration。

### [T2-1] Entity — `src/TCS.Core/Entities/TrainingDetail.cs`

- [x] `decimal Hours` → `decimal? Hours`

### [T2-2] DTO — `src/TCS.Core/DTOs/TrainingDetailDto.cs`

- [x] `decimal Hours` → `decimal? Hours`（record positional parameter）

### [T2-3] Request — `src/TCS.Core/DTOs/Requests/CreateTrainingDetailRequest.cs`

- [x] `decimal Hours` → `decimal? Hours`

### [T2-4] Request — `src/TCS.Core/DTOs/Requests/UpdateTrainingDetailRequest.cs`

- [x] `decimal Hours` → `decimal? Hours`

### [T2-5] Validator — `src/TCS.Core/Validators/Training/CreateTrainingDetailValidator.cs`

- [x] 改為「有值才驗範圍」：
  ```csharp
  RuleFor(x => x.Hours)
      .GreaterThan(0m).WithMessage("受訓時數必須大於 0")
      .LessThanOrEqualTo(9999.9m).WithMessage("受訓時數不可超過 9999.9")
      .When(x => x.Hours.HasValue);
  ```

### [T2-6] Validator — `src/TCS.Core/Validators/Training/UpdateTrainingDetailValidator.cs`

- [x] 同 T2-5，加上 `.When(x => x.Hours.HasValue)`

### [T2-7] DB（使用者自行執行）

- [x] `ALTER TABLE TCSTB ALTER COLUMN TB005 decimal(6,1) NULL`

### [T2-9] 前端 — 時數僅在「回訓」時必填

> 原 `submitDetail` 無條件要求時數 > 0；`Index.cshtml` 的 `#m-Hours` 固定帶 `required`。
> 需改為：選「取得證照（1）」時時數可空，選「回訓（2）」時才必填。

- [x] `Index.cshtml:237-238` — `*` 號加上 `id="m-Hours-required" d-none`（預設隱藏）；移除 input 的 `required` 屬性
- [x] `training.js` — 新增 `syncHoursRequired()`，依目前 TrainingType 切換 `*` 顯示
- [x] `training.js` — `openDetailModal` 結尾呼叫 `syncHoursRequired()`
- [x] `training.js` — `input[name="m-TrainingType"]` change 事件綁定 `syncHoursRequired()`
- [x] `training.js` — `submitDetail` 改為：Hours 空值送 `null`；僅 TrainingType === 2 時強制 Hours > 0

### [T2-10] 後端 — 時數僅在「回訓」時必填

- [x] `CreateTrainingDetailValidator.cs` — 新增 `NotNull` 規則，`.When(x => x.TrainingType == 2)`
- [x] `UpdateTrainingDetailValidator.cs` — 同上

### [T2-8] 驗證

- [x] `dotnet build` 0 errors
- [x] 新增 TrainingDetail 時不填時數 → 存檔成功，TB005 為 NULL
- [x] 新增 TrainingDetail 並填時數 → TB005 有值
- [x] 填入負數或超過 9999.9 → 驗證錯誤正常回傳
- [x] 選「取得證照」時不填時數 → 存檔成功
- [x] 選「回訓」時不填時數 → 前後端均攔截，回傳錯誤訊息
- [x] 選「回訓」並填時數 → 存檔成功

---

## T3 — 員工搜尋補齊 MV022 / MV023 預設篩選條件

> 規格（`todo-2026-05-21-2.md` T3-0）要求預設套用：
> `MV001 not like '3%' and MV001 like '[0-9][0-9][0-9][0-9]' and MV022='' and MV023=''`
>
> 目前 `EmployeeRepository.SearchAsync` 已實作前兩條，
> 但 `MV022=''` / `MV023=''` 完全未實作，原因是 `Employee` Entity 和 Configuration 均未映射這兩個欄位。

### [T3-1] Entity — `src/TCS.Core/Entities/Employee.cs`

- [x] 新增屬性：
  ```csharp
  public string? MV022 { get; set; }
  public string? MV023 { get; set; }
  ```

### [T3-2] EF Configuration — `src/TCS.Infrastructure/Configurations/EmployeeConfiguration.cs`

- [x] 新增欄位映射（型別待確認後補充 HasMaxLength / IsFixedLength）：
  ```csharp
  builder.Property(e => e.MV022).HasColumnName("MV022").IsUnicode(false);
  builder.Property(e => e.MV023).HasColumnName("MV023").IsUnicode(false);
  ```

### [T3-3] Repository — `src/TCS.Infrastructure/Repositories/EmployeeRepository.cs`

- [x] 在 `SearchAsync` 的 `Where` 中補上兩條預設篩選（與現有條件 AND 連接）：
  ```csharp
  (e.MV022 == null || e.MV022 == "") &&
  (e.MV023 == null || e.MV023 == "") &&
  ```

### [T3-4] 驗證

- [x] `dotnet build` 0 errors
- [x] 搜尋結果不含 MV022 / MV023 有值的員工（依業務邏輯確認）

---

## 驗證清單

- [x] `dotnet build` 0 errors
- [x] `GET /tcs/api/employees/search?q=2213` 回傳員工清單（非 404）
- [x] Employee Picker 搜尋正常運作
- [x] Edit Header 時員工欄位正常顯示姓名
- [x] TrainingDetail 新增時 Hours 可為空，也可填值，兩種情況均正常存檔
