# TCS 修正 TODO List

| 項目 | 日期 | 狀態 |
|---|---|---|
| 建立日 | 2026-05-21 | — |
| 完成日 | 2026-05-21 | 待 DB update |
| 依據 | 使用者需求（2026-05-21） |

---

## 規格釐清紀錄

| # | 議題 | 結論 | 影響 |
|---|---|---|---|
| 1 | IsExpired 已廢棄，不需要排程器 | 移除 `TrainingDetail.IsExpired` 欄位及 `ExpiryScanService` | T1 全部移除，不保留任何殘留 |
| 2 | COMPANY / USR_GROUP 補齊範圍 | 所有 4 個實體一律補齊；COMPANY = 硬編碼 `"BERLIN"`，USR_GROUP = JWT `"department"` claim | 須在 `ICurrentUserService` 新增 `UsrGroup` 屬性；COMPANY 不從 JWT 取得 |
| 3 | schema.json 資料表名稱衝突 | schema.json 用 TCSMA/TCSMB/TCSTA/TCSTB；實際 DB 為 TRNM01/TRNM02/TRNF01/TRNF02 | 擇一對齊：更新 schema.json，或規劃資料表改名 Migration |

---

## T1 — 移除 IsExpired 欄位及 ExpiryScanService

> `TrainingDetail.IsExpired` 已不需要，排程器（`ExpiryScanService`）隨之廢棄。
> 須同步移除所有引用，避免留下死碼。

### [T1-1] 後端 Entity — `src/TCS.Core/Entities/TrainingDetail.cs`

- [x] 移除 `public bool IsExpired { get; set; }` 屬性

### [T1-2] 後端 Services — `src/TCS.Infrastructure/Services/`

- [x] 刪除 `ExpiryScanService.cs`
- [x] 若有獨立的 `IExpiryCalculator` 介面及其實作，一併刪除

### [T1-3] 後端 DI 註冊 — `src/TCS.Web/Program.cs`（或 ServiceCollectionExtensions）

- [x] 移除 `AddHostedService<ExpiryScanService>()` 及相關 DI 註冊
- [x] 移除 `IExpiryCalculator` 的 DI 綁定（若存在）

### [T1-4] EF Migration

- [x] 執行 `dotnet ef migrations add RemoveIsExpiredFromTrainingDetail --project src/TCS.Infrastructure --startup-project src/TCS.Web`
- [x] 確認 migration 只包含 `DropColumn(table: "TRNF02", name: "IsExpired")`
- [ ] 執行 `dotnet ef database update`

### [T1-5] 驗證

- [x] `dotnet build` 0 errors
- [x] 確認 `grep -r "IsExpired" src/` 無任何殘留引用
- [x] 確認 `grep -r "ExpiryScan\|IExpiryCalculator" src/` 無任何殘留

---

## T2 — 補齊缺失的系統欄位（COMPANY、USR_GROUP）

> `schema.json` 定義的 7 個系統欄位中，`IAuditableEntity` 目前只涵蓋 5 個：
> Creator、CreateDate、Modifier、ModiDate、Flag。
> **COMPANY** 和 **USR_GROUP** 完全缺失，需補入介面及所有實體。

### [T2-1] 介面 — `src/TCS.Core/Common/IAuditableEntity.cs`

- [x] 新增：
  ```csharp
  string? Company { get; set; }
  string? UsrGroup { get; set; }
  ```

### [T2-2] 實體 — 4 個實體一律補齊

- [x] `LicenseMaster.cs`：新增 `public string? Company { get; set; }` 及 `public string? UsrGroup { get; set; }`
- [x] `LicensePlantRequirement.cs`：同上
- [x] `TrainingHeader.cs`：同上
- [x] `TrainingDetail.cs`：同上

### [T2-3] EF 設定 — `src/TCS.Infrastructure/Configurations/`

各設定檔加入欄位對應（`varchar(10) nullable`）：

- [x] `LicenseMasterConfiguration.cs`：
  ```csharp
  builder.Property(e => e.Company).HasColumnName("COMPANY").HasMaxLength(10).IsUnicode(false);
  builder.Property(e => e.UsrGroup).HasColumnName("USR_GROUP").HasMaxLength(10).IsUnicode(false);
  ```
- [x] `LicensePlantRequirementConfiguration.cs`：同上
- [x] `TrainingHeaderConfiguration.cs`：同上
- [x] `TrainingDetailConfiguration.cs`：同上

### [T2-4] EF Migration

- [x] 執行 `dotnet ef migrations add AddCompanyAndUsrGroupToAuditableEntities --project src/TCS.Infrastructure --startup-project src/TCS.Web`
- [x] 確認 migration 對 4 張資料表各新增 `COMPANY` 及 `USR_GROUP` 欄位
- [ ] 執行 `dotnet ef database update`

---

## T3 — 系統欄位自動填入（SaveChanges 攔截器）

> 目前 `AppDbContext` 沒有 `SaveChangesAsync` 覆寫，系統欄位（Creator、CreateDate、
> Modifier、ModiDate、COMPANY、USR_GROUP、Flag）在任何 CRUD 操作中從未被填入，
> 導致 DB 中這些欄位永遠為 null。
> 應在 DbContext 層統一攔截，避免每個 Service 分散處理。

### [T3-1] 後端 `src/TCS.Infrastructure/Data/AppDbContext.cs`

- [x] 注入 `ICurrentUserService`（來自 `ERP.Auth.Common`）
- [x] 覆寫 `SaveChangesAsync`：
  ```csharp
  public override Task<int> SaveChangesAsync(CancellationToken ct = default)
  {
      var now = DateTime.Today.ToString("yyyyMMdd");
      var user = _currentUserService.UserId ?? "SYSTEM";

      foreach (var entry in ChangeTracker.Entries<IAuditableEntity>())
      {
          if (entry.State == EntityState.Added)
          {
              entry.Entity.Creator = user;
              entry.Entity.CreateDate = now;
              entry.Entity.Company = "BERLIN";                     // 硬編碼公司代碼
              entry.Entity.UsrGroup = _currentUserService.UsrGroup; // JWT "department" claim
              entry.Entity.Flag = 1;    // 建立 = 第一次修改
          }
          if (entry.State is EntityState.Added or EntityState.Modified)
          {
              entry.Entity.Modifier = user;
              entry.Entity.ModiDate = now;
          }
      }
      return base.SaveChangesAsync(ct);
  }
  ```
- [x] 在 `ERP.Auth.Common` 的 `ICurrentUserService` 新增 `string UsrGroup { get; }`，`CurrentUserService` 讀取 `FindFirstValue("department")`
- [x] `COMPANY` 不需要從 `ICurrentUserService` 取得，直接寫死 `"BERLIN"`

### [T3-2] 驗證

- [x] `dotnet build` 0 errors
- [ ] 新增一筆 LicenseMaster → DB 中 CREATOR、CREATE_DATE、COMPANY 均有值
- [ ] 修改同一筆 → MODIFIER、MODI_DATE 更新，CREATOR 不變

---

## T4 — schema.json 資料表名稱對齊（建議討論）

> `schema.json`（最近才新增）使用 TCSMA / TCSMB / TCSTA / TCSTB，
> 但實際 EF 設定及 DB 資料表為 TRNM01 / TRNM02 / TRNF01 / TRNF02，
> 兩者不一致會造成維護混亂。
> **請確認方向後擇一執行**：

| 選項 | 做法 | 成本 |
|---|---|---|
| A（推薦）| 更新 `schema.json` 的 table 欄位，改用 TRNM01 等實際名稱 | 低，只改文件 |
| B | 新增 EF Migration 將 TRNM01→TCSMA 等資料表改名 | 高，需停機操作及更新 EF 設定 |

- [x] 確認選項 A（已更新 schema.json 表名：TCSMA/TCSMB/TCSTA/TCSTB → TRNM01/TRNM02/TRNF01/TRNF02）
- [x] 同時確認 `schema.json` 欄位名稱（MA001、MA002…）是否與實際 DB 欄位名稱一致；若不一致則補充對照
  > **注意**：schema.json 欄位名採舊式 ERP 代碼（MA001、MB001…），實際 DB 欄位名與 EF 屬性名相同（LicenseType、Description…），兩者不一致。業務欄位命名差異已確認並留存，如需對齊須另行規劃。

---

## T5 — FLAG 欄位語義定義

> `FLAG decimal(3,0)` 語義已確認：**使用者修改次數計數器**。
> 建立時 FLAG = 1（視為第一次修改），每次 UPDATE 自動 +1，達 999 後循環歸 1。
> 不用於軟刪除，查詢無需加過濾條件。

- [x] 確認 FLAG 語義：修改次數計數器（建立 = 1，每次 UPDATE +1，999 → 循環歸 1）
- [x] `AppDbContext.SaveChangesAsync` 已更新：Added → Flag = 1；Modified → (Flag+1 > 999 ? 1 : Flag+1)
- [ ] EF Migration：將 FLAG 欄位從 `decimal(1,0)` 改為 `decimal(3,0)`（4 張資料表）
- [ ] 執行 `dotnet ef database update`

---

## 驗證清單

- [x] `dotnet build` 0 errors
- [ ] `dotnet ef database update` 成功（需在可連線 SQL Server 的環境執行）
- [x] `grep -r "IsExpired\|ExpiryScan\|IExpiryCalculator" src/` 無任何輸出
- [ ] 新增 LicenseMaster → DB 確認 CREATOR / CREATE_DATE / COMPANY / USR_GROUP / FLAG 均有值
- [ ] 修改 LicenseMaster → DB 確認 MODIFIER / MODI_DATE 更新、CREATOR 不變
- [x] schema.json 表名與實際 DB 一致（T4 完成後）
